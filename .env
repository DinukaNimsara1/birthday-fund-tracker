# Frontend Configuration
# Update this with your Azure backend API URL after deployment
VITE_API_URL_DEV=http://localhost:4000
# Environment Mode (development or production)
# Controls which API URL to use
VITE_ENV=production

# Development API URL (used when VITE_ENV=development)
# Defaults to http://localhost:4000 if not set
VITE_API_URL_DEV=http://localhost:4000

# Production API URL (used when VITE_ENV=production)
# Example: https://your-backend.azurewebsites.net
VITE_API_URL=https://bdayfund-tracker-server.azurewebsites.net